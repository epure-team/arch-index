# Build output
callgraph-go/arch-callgraph-go
callgraph-go/effects/arch-effects-go
_build/
_opam/
# Per-target call-graph artifacts — never committed (target-specific)
*.db
# MCP / tooling scratch
.playwright-mcp/
# Agent worktrees: transient full checkouts the harness creates in-tree when a
# subagent runs isolated. Committing them would nest the repo inside itself.
.claude/
