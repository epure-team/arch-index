# Build output
callgraph-go/arch-callgraph-go
callgraph-go/effects/arch-effects-go
callgraph-rust/target/
_build/
_opam/
# Per-target call-graph artifacts — never committed (target-specific)
*.db
# MCP / tooling scratch
.playwright-mcp/

# Locally-built 23 MB artifact; never commit (CI cannot run a foreign-glibc binary)
/bin/arch-callgraph-ocaml
