# Ship gate — OCaml CFA foundation, stage2

2026-09-16. Branch `feat/ocaml-cfa-foundation`, target `main`.

Review GO round2/cycle1 and QA GO round1/cycle1. QA product head8eb28f4,
equivalent to reviewed791d1f9 for source/check/config files. Subsequent changes
are pipeline evidence only. Full345/345 and native kernel6/6; CHECK1/2/3 plus
pure controls pass with286source and5binary identity checks.

Finite same-CMT function-target/reason domain and worklist; supported literals,
aliases and branch-result joins flow to authentic MAY targets. Opaque root
aliases retain unknown alongside known targets. No general call/return/capture,
partial closure, recursive-value, functor or variant transfer in this stage.

Pinned410 Tezos replay: Irmin4849→4850, protocol12171→12173, zero losses and
newMUST.45054 rows; resource observation2.842s/145152KiB. Exact accounting,
not a soundness/completeness proof. Self references6808calls/603origins were
explicitly approved and freshly attributed with exact2x2 equality; no rule or
new exemption change. Existing single allowance coordinate updated only.

Two medium/one low architecture advisories remain disclosed in round2 ledger
and roadmap for stage3 planning. OpenCode degraded timeout then shared-breaker
skip; no cross-runtime approval. Original pre_fix_sha null: real manualRED
recorded, no automatedRED attestation. Spec execution fallback to root after
independent Terra prep and subagent thread limit is explicit.

User authorized autonomous Roster stages, scoped commits, PR, CI fixes and
green guarded rebase merge. Routine quizzes delegated; no answers fabricated.
Seven foreign untracked paths are preserved/excluded, as explicitly directed.
No new worktree; owned calibration archive trees removed. Existing unrelated
worktrees and PR93 remain untouched. Stage1 delivery documentation commita4b4df5
is included ahead of stage2 because local main carried its unpushed receipt;
it changes no product source. No issue closure is invented for stage2.

Pending: open PR, verify required GitHub build on its exact final HEAD,
guarded rebase merge, verify merged source and update roadmap/delivery receipt.
Local GO does not substitute for CI. No stage3 implementation before merge.
