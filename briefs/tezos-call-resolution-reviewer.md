# Reviewer — tezos-call-resolution

**Status: VALIDATED**

Review the completed implementation of same-CMT structured-module function-body targets plus fixed410 comparison. Read implementation brief, plan and specs/tezos-call-resolution.md before audit. The user's explicit scope amendment includes the six existing compatibility/origin evidence files listed in the implementer brief; independently review their recalibration without weakening the gates.

Audit first: arch_index_cmt.ml/.mli ownership/member export selection and ordinary/callback/point-free head construction; call_graph_extractor.ml same-file attribution; new local_module_targets native tests; roster/tezos-call-resolution comparator/runner and mutation checks. Verify compiler identities are not printed/global stamp keys, all intermediate owners are concrete, includes/shadowing mask earlier bodies, target uses existing stored binding name, actual syntactic arity controls partial/return residuals, no new MUST and no lost pending metadata. Verify dropped targets remain TOP, aliases/parameters/applications/unpacks refuse, and flat fallback does not invent nested coverage/cross-file targets.

Inspect every changed fixed410 group and source/target witness; relation count is not semantic proof. Assert duplicate-preserving comparison, unchanged-input provenance, no existing-target loss, no generic allowlist for TOP changes, no count inflation from value_alias. Check checkers distinguish assertion/setup errors and fail on incomplete data. Confirm docs state limitations and no 0CFA/general defunctorization claim.

Run full roster-review including its installed independent review gates and required fix-first loop; then QA. Preserve unrelated dirt and PR93. No merge until all required exact-head CI succeeds. Scope is enumerated in implementer brief and manifest; never legitimize an out-of-scope edit merely by adding it to review.
