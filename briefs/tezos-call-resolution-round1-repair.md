# Round1 NO-GO repair sub-brief

Revalidated existing scope and plan, under the user's autonomous delegation.
All three arity observations refer to one invariant; use the same new ratchet
check for them. No speculative next product iteration until this one is shipped.

1. Add self-contained `roster/tezos-call-resolution/check-labeled-arity.js` that
   compiles its own fixture/probe in one owned temporary directory against the
   exact checkout build. Check native argument-slot premises, optional context
   absent/present, pending partial metadata and exact head/residual counts.
   Observe genuine assertion RED before changing the producer. Integrate this
   check into the existing local_module_targets native registration/dependencies.
2. Count present arguments only for newly known local structured-module bodies
   in arch_index_cmt.ml saturation/residual accounting. Preserve old non-owned
   head and noreturn/CFG logic. No module alias/functor substitution expansion.
3. Add `roster/tezos-call-resolution/check-verifier-inputs.js` and isolated tests
   for FR014 refusals in comparison.js/verify.js. Prefer the smallest internal
   test seam if needed, with unchanged CLI/pinned defaults and no real corpus or
   baseline mutation. Include valid controls, assertion/setup distinction and
   cleanup; tests of implemented refusal behavior may already pass.
4. Rebuild, run all5 spec checks, full guards and fixed410 exact witness verify.
   If canonical rows differ, do not reuse a mismatching witness or bypass the
   refusal: regenerate/review exact compiler evidence first. Recalibrate only
   authorized six-file self/compatibility evidence when justified by exact diff.
5. Declare ratchets in impl, document actual results, deactivate owned task slot,
   commit only owned changes, and run roster review round2 with full scrutiny of
   visible arity behavior correction. Cross-runtime degraded breaker remains.

Original base/dirty manifest header remains pinned; add only explicitly allowed
test dependency path tezt/tests/dune if integration uses it. Never record task
changes as pre-task dirt. Preserve unrelated dirt, PR93 and old worktrees.
