# Ship gate — tezos-local-recursion

Review GO (round1/cycle1), QA GO (round2/cycle1). Standing explicit user autonomy
authorizes this PR, exact-head CI repair in scope and guarded rebase merge before
iteration5. No fabricated quiz answers. Seven unrelated user files stay untouched.

Branch feat/tezos-local-recursion, fetched origin/main05e4a8a7ba2d64ff08a15e268005f800e06ea4d7.
Product3181262, test-only coverage60bf7ab, final review76fe1b7. Subsequent changes
are pipeline documentation only. One capability: singleton plain-variable local
recursive direct-literal self-invocation heads, inside the exact RHS, reach actual
observed collision-qualified stored bodies as MAY_ENUMERATED. No new MUST,
public API/schema/flat change, general0CFA, mutual groups or functor expansion.

Fixed410 comparison: +68Irmin/+111protocol,179 relations/181 witnessed heads,
45052rows each side, zero loss/unexplained/new MUST/removed legacy TOP residual.
Native compiler witnesses and counted capacities are independent of candidate
targets. Four exact reference updates follow pristine source-only A=B/C=D proof;
thresholds/allowance counts and old checkers remain unchanged.

Three final personal review runs passed build/full340/CHECK1–6; main final QA
also passed all gates. First QA run hung on unchanged TypeScript polyglot LSP;
operator terminated the owned CLI. The unchanged focused test then passed, as did
one fresh full340 QA. Failure history and unresolved intermittent LSP risk are
retained, not claimed fixed. Cross-runtime review non-conforming output was
discarded; QA respected its degraded breaker. Formatter/coverage/KB absent.
Global claims parser fails older out-of-scope syntax; no global PASS claimed.

Native Tezt cases are portable CI tests. Fixed410/baseline/calibration raw evidence
is intentionally local, ignored and pinned; the task-local CHECK1–6 corpus gate
is not advertised as standalone on an arbitrary fresh clone. Local-only Git ref
retains the calibration object; owned diagnostic worktrees/builds are removed.

Push normally, no force. Required build must be SUCCESS on exact PR head before
match-head guarded rebase merge. No admin/bypass. HeldPR93 and foreign worktrees
are excluded. No fourth KEEP until actual merge. PR/CI/merge pending at writing.

## Delivery confirmation

PR108 https://github.com/epure-team/arch-index/pull/108 rebase-merged at
2026-09-15T01:45:05Z to4a952441fcd7648d20e68b5c6348de1b3fe85522.
Required build SUCCESS11m00 on exacthead24711ef44cfc341f60a4584bf65a406764df5282;
CI https://github.com/epure-team/arch-index/actions/runs/34917720591.
MCP and release were explicitly skipped. No bypass/admin/force; match-head guard.
Merged tree exactly matches checked head (git diff --quiet exit0).

Local main initially retained one prior delivery-doc commit82c5a81; git cherry
proved it already applied upstream, then normal rebase resynchronized main to
origin/main. Feature branch removed. Seven unrelated user files preserved;
zero owned calibration worktrees remain. Failed QA temporary directory/processes
were already cleaned by the test runner; raw small logs remain as audit evidence.

Attempt4 KEEP: retainedIrmin4849/protocol12157, +68/+111 this iteration, zero loss.
Cumulative gain vs original baseline:+477Irmin/+937protocol. Results and external
roadmap updated after merge. Four of the exact five iterations delivered; fifth
not started at this record. Intermittent unrelated LSP risk remains disclosed.

Post-ship hygiene: all9 ledger phases have matching friction entries. The skill's
uncompiled .js path is absent and direct .ts execution is incompatible here;
the existing dist/scripts/check-friction-shape.js validated66 current entries,
210 older entries skipped, exit0. No tool install or upstream edit was needed.
Harness/metabolism/KB are absent; the pre-existing user cost file was not touched.
