# Plan — tezos-open-bodies

**Date:** 2026-09-14
**Status: VALIDATED**

## Source and gate

Sole decomposition source: validated intake, read in full. Spec brief consulted
only for its VALIDATED gate. Fresh sequential voices open_plan_sol (Sol) then
open_plan_terra (Terra) read only intake, no code or spec. No Claude runtime
voice claimed. Routine validation uses explicit user autonomy; no quiz answers
invented (human-validation.md absent). No capability expansion approved here.

## Sequential steps

1. **Freeze and characterize the invocation slice.** Freeze exact writable paths
   and pre-task dirt before baseline builds. Recheck runnable build/test/bundle
   and the distinct PR106 baseline. Add native Tezt/check fixtures for the exact
   structural open/function shape, actual body identity and refusals. Execute a
   genuine producer assertion RED, not a setup failure. No product edits yet.
2. **Deliver safe direct invocation end to end.** In arch_index_cmt.ml introduce
   the separate invocation-only context, join exact same-CMT compiler identity,
   and resolve to the existing actual lambda. Include missing/ambiguous/rejected
   target refusal, syntactic arity/supplied arguments, partial and returned-call
   residual semantics in this same slice. Keep all legacy ownership and flat
   output unchanged; adjust the flat consumer only if a test proves necessary.
   Completion: native positive/refusal/arity/ownership/flat checks and full guard.
3. **Demonstrate retained real-corpus gain.** Build a task-specific independent
   native admission/witness check, compare fixed410 candidate to frozen PR106
   multiset, and prove every changed occurrence/residual. Refuse ambiguous
   evidence, any old relation loss or unexplained movement. Self-reference
   refresh only after unchanged-policy pristine attribution. Strictly positive
   gain is necessary to retain; neutral/negative discards owned product edits,
   never widens recognition to force gain.
4. **Review, QA and ship this attempt.** Full roster review and QA, exact-head
   required green CI then SHA-guarded rebase merge. Record actual results and
   roadmap, preserve PR93/unrelated dirt, remove only owned disposable artifacts.
   Do not start attempt4 before this attempt's outcome is recorded and any kept
   change merged. These four steps count as one product attempt, not four.

## Dependencies

1 precedes product edits in2. Native safety and compatibility in2 precede gain
admission in3. All verification in3 precedes review/QA/ship in4. Correction
rounds remain bounded by roster; no later attempt stacks on speculative code.

## Consensus Table

| Point | Sol | Terra | Status |
|---|---|---|---|
| Freeze baseline/scope before edits | required | required | AGREE |
| Exact identity; actual body; no name guessing | required | required | AGREE |
| Separate invocation context, never global widening | required | required | AGREE |
| Arity/residual/flat/ownership are mandatory | required | required | AGREE |
| Independent native admission and positive gain | required | required | AGREE |
| Discard neutral rather than broaden | required | required | AGREE |

Both voices proposed intermediate descriptor/integration layers; main bundles
them into one independently tested vertical capability in step2, with safety
present from first emission. This is sequencing, not an alternative scope.
Neither voice recommended a direction change. No DISAGREE/USER-CHALLENGE remains.

## Identified risks

| Risk | Probability | Impact | Mitigation |
|---|---|---|---|
| Descriptor name exists but actual body does not | medium | false target | explicit availability/refusal tests |
| Shadowing or nested modules mix binders | medium | false target | same-CMT compiler identity, native shadow tests |
| Partial/overapplication misclassification | medium | unsound precision | supplied count, no new MUST, witnessed residual |
| Shared collector changes flat/effects | medium | regression | paired exact output and ownership checks |
| Evidence counts hide duplicate-site changes | medium | false gain | independent occurrence/multiset admission |
| No actual corpus gain | possible | discard | fixed keep rule, no scope widening |

## Decisions made

Use existing canonical body identity with explicit refusal; deriving a descriptor
is not permission to emit a guessed target. All arity/identity details are test
obligations before new target emission. The independent witness tool is new
task-specific validation, not the documentary census or product lookup. Native
fixtures complement the fixed corpus for negative cases. Any need for a new
product file outside the intake's expected area requires intake clarification
before editing; no global consumer fix is pre-approved.

## Assumptions

Intake's existing structural traversal, body ownership and supplied-argument
information are usable without widening recognition. If disproved, refuse or
return to intake, not synthesize new capability. Fixed corpus remains unchanged.
No KB/claims reconciler/hooks installed; absence is not a successful validation.

## Implementation-time reference inventory correction

The actual full guard exposed two duplicated exact reference constants in
checks/origin-recurring-consumer.js (totals and sole allowed assertion identity).
That one collateral check path is now frozen in the manifest before any edit.
Updating it after pristine attribution is part of step3's existing exact-reference
refresh, not a new feature or a policy change. All other check assertions remain
unchanged. Details and actual failing evidence are recorded in
roster/tezos-open-bodies/self-reference-investigation.md.
