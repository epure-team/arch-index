# Reviewer — tezos-open-bodies

**Date:** 2026-09-14
**Status: VALIDATED**

## Expected implementation (not yet delivered)

Only direct same-CMT Pident invocation targets for structural variable bindings
whose RHS is a single path-only lexical open immediately around a literal
function. Existing actual lambda body remains owner of its calls/effects. No
global recognition widening or new MUST. Read validated intake, spec and eventual
impl brief; absence of implementation evidence is not GO.

Audit lib/arch_index/arch_index_cmt.ml first, any conditional flat consumer diff
second, then native tests and independent task witness/checkers. Verify all34FR
and13AC, especially actual persisted body vs guessed name, exact binder shadow
identity, omitted arguments/partiality, one unknown residual per overapplication,
flat multiset and effect/CFG ownership equality, duplicate witness consumption,
missing/dropped/ambiguous refusal and unchanged point-free/alias/callback paths.
Evidence oracle must not derive admissibility from product target lookup or
documentary census. Retained gain must be positive on fixed410, zero prior losses,
zero unexplained movement, unchanged guard policy. Any exact self refresh needs
pristine attribution, not larger allowances. No Tezos/schema/publicCLI/PR93 or
prior task verifier edits. Follow full roster-review with real check traces;
then QA, exact-head required CI and guarded rebase merge, not before.

Build/full test/bundle/baseline/diff commands are repeated in qa-scope. Run the
spec's exact executable checks yourself as required, do not infer from logs.
Known absent hooks/claims/formatter/coverage must be reported, not marked passing.

Full-guard investigation found duplicate exact reference assertions in
checks/origin-recurring-consumer.js. Its exact path was added to the manifest
before any edit, under the intake's conditional reference-refresh authority.
Review only allows updating those two expected constants after attribution;
all other checker logic and policy cases must remain byte-identical. Verify the
sole assertion's source identity survives its implementation-owner rename and
coordinate relocation, with x1 unchanged; no new allowance is authorized.
