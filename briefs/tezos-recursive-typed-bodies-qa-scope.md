# QA scope — tezos-recursive-typed-bodies

**Status: VALIDATED**
**Date:** 2026-09-15

Fifth/final attempt. No web/TUI changes. Exact singleton recursive named-path
open/direct-function self-head targets only; MAY_ENUMERATED after original-root
observation and storage. All neighboring refused forms and predecessor facts
remain unchanged. Native identity and SQL storage are separate proof obligations.

Run serially with rtk proxy from /home/mathias/dev/arch-index:

```sh
opam exec -- dune build
opam exec -- dune exec tezt/tests/main.exe -- --no-color
node scripts/review-bundle-verify.js
git diff --check
node roster/tezos-recursive-typed-bodies/prepare-baseline.js --check
```

Before product edits, implementation must add the exact task check commands
required by the spec and record authentic semantic RED separately from setup
failure. QA reruns the completed command mapping, including independent native
witness validation, immutable baseline refusal/tampering cases, fixed410 gain,
complete rich/flat compatibility and residual/duplicate capacity controls.

Require non-vacuous exception/channel/effect/context fixtures, caller and range
preservation, optional/default/refutable and nested callable tests, conservative
failed/dropped roots, collision naming and direct recursive regressions. No
ordinary table or literal mask changes, parent occurrence removals or new MUST.
Sealed v2 must remain pinned and read-only; original invalid package cannot be
accepted. Fixed410 comparison requires positive distinct relation gain, zero
loss and only independently justified head/residual changes. Fourteen observed
heads alone are not a passing metric. No formatter/full lint/coverage configured.

Record actual full-test result and exact tested identities. Do not silently
waive or overwrite an intermittent LSP hang; use roster QA convergence and
bounded rerun rules. No report/source/git writes during any build/check. Review
and QA GO precede PR shipment; required CI must succeed on exact final head,
followed by guarded rebase merge before final KEEP/result/roadmap update.
