# Reviewer — tezos-recursive-typed-bodies

**Status: VALIDATED**
**Date:** 2026-09-15

Review target (not an implementation-complete claim): fifth/final attempt admits
only singleton plain-variable recursive RHS Texp_open(Tmod_ident, immediate
Texp_function), and only direct matching self-head invocations in rich processing.
Audit lib/arch_index/arch_index_cmt.ml first, then dedicated native tests and
roster/tezos-recursive-typed-bodies witness/comparison/baseline tooling. Read the
validated intake/spec and eventual implementation evidence; no pre-awarded GO.

Confirm original physical inner root, Ident.same scope, actual collision-qualified
allocation, exactly-one observation plus successful storage; otherwise TOP.
Only MAY_ENUMERATED, no new MUST. Existing direct recursion, parent occurrences,
continuations, non-head uses, binding_literals/local_lam_stamps and flat output
must be unchanged. Arity counts supplied Some arguments and preserves result
arrow partiality and at-most-one returned-call residual including legacy cases.

Audit evidence independence: compiler witness must not consume candidate SQL or
inferred targets. SQL storage evidence cannot establish native identity. Match
complete duplicate groups and single-use capacities, not loose location/name
sets. Preserve complete rich context, exceptions/channels/effects and non-call
facts with non-vacuous fixtures. Baseline must be sealed v2, exact fixed410 pins;
invalid original is never accepted. Candidate gains are distinct relations,
positive with zero loss, not fourteen presumed gains.

Run authentic build, full Tezt, review bundle, diff check, baseline check and all
task-native/witness/comparison/preservation/capacity checks. Record exact source,
checker, producer, schema and input identities and actual command outcomes.
Serialize checks against writes. Review runtime limitations are not PASS. No
public mli/schema/Tezos/old checker changes or unrelated dirt. No self-reference
change without pristine attribution and explicit path authorization. Required
CI on exact final head is mandatory even if local documentation-only gate reuse
is justified. Full roster review/convergence before QA and guarded rebase merge.
