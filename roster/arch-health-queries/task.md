# Task — arch-health-queries

Add a code-health query pack to arch-index's arch-query CLI (and schema where needed), consolidating features proven in the sibling implementations: (1) duplicate-function detection via body hashing (reference: ~/dev/epure/src/arch_index/arch_index_compare.ml — note arch-index already has lib/arch_index/arch_index_compare.ml with duplicate-body detection, check what's exposed); (2) unsafe-params tracker — raw primitive (string) params repeated across signatures that should be domain types (references: ~/dev/octez-manager/tools/arch_query.ml unsafe-strings cmd :1035 + unsafe_params table, ~/dev/miaou/docs/architecture-schema.sql lines 88-101); (3) god-module / large-file / large-function / missing-docs health queries with tunable thresholds (reference: ~/dev/octez-manager/tools/arch_query.ml :1001-1035); (4) type-shape search — find types by field names and field types (reference: ~/dev/octez-manager/tools/arch_query.ml type-search cmd :986, type_fields/type_constructors tables which arch-index's schema already has). Read-only queries over existing schema where possible.

Item 3 of the 5-item consolidation roadmap (item 1 = PR #3, item 2 = PR #4 stacked).
