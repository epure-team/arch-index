<!-- No title: the path already identifies this file. -->

_Generated: 2026-09-03_
_DO NOT include the task description in this file or share it with the researcher._

1. What is the surface of the Tezos protocol environment's error monad as seen by `proto_alpha`: which environment version its `dune` selects, and in `src/lib_protocol_environment/sigs/<version>/error_monad.mli` (and the generated `v<N>.ml`), the exact declarations and paths of `error`, `fail`, `tzfail`, `error_with`, `catch`, `catch_e`, `protect`, `record_trace`, `trace`, `trace_eval`, the `tzresult` type (how it relates to `result` and `trace`), `register_error_kind`, and the `Lwt_result_syntax` / `Result_syntax` / `Lwt_syntax` binding operators? Cite file:line; also state how these paths print in the `.cmt` (persistent unit name).
2. How do binding operators (`let*`, `and*`, `>>=?`, `>>?`) and applications of `Result.bind` / `Option.bind` appear in the OCaml 5.3 Typedtree, and how does `lib/arch_index/arch_index_cmt.ml` handle them today (`Texp_letop`, `bop_op_path`, `add_path_call`, the CFG lowering of the body as a conditional region)? Does the walker record the operator's resolved path, and what does `calls` store for a `let*` site?
3. How are `Result`/`Option` values matched today: what does the walker do with `Texp_match` arms `Error e` / `Ok v` / `None` / `Some x` (patterns on ordinary constructors, `Tpat_construct` on non-extension constructors), and does any existing code inspect a function's return type (`Types.type_expr`, `Tarrow` result, `Tconstr` paths) — e.g. `arrow_arity`, `is_arrow`, `extract_types_from_signature`, `type_to_string`? Cite file:line.
4. What is the current structure of the exception-identity feature on this branch: the `exn_*` tables and `comment_db_meta.exn_contract` in `architecture-schema.sql`, `lib/arch_index/arch_index_exn.ml` (recognisers, `canonical_path`, scope/origin recording, `finalize`), the hooks in `arch_index_cmt.ml` (`lexn`, `enter_scope`, `record_raise_head`, `record_prim_head`, `process_cmt` inserts, `unit_declared`), `lib/arch_tools/arch_exn.ml` (lattice, `load`, `close`, `direct`, `contribution`, `solve`, `known_leaf`, `rows_for`), and the `raises`/`raisers-of`/`exn-stats` arm in `bin/arch_query/arch_query.ml` (flag parsing, `need_contract`, `Arch_fmt.print`)? Which parts are keyed on the literal string "exception" or on the `exn` type, and which are generic?
5. Which configuration and data-file formats does the repository already parse, with what libraries: `arch-rules.txt` (grammar, discovery, parser location in `bin/arch_rules`), `dune-project` / `*.opam` dependencies (is any TOML library — `toml`, `otoml` — a dependency, or only `yojson`?), the NDJSON contract in `lib/arch_db/arch_load.ml`, and how `bin/arch_effects_load` / `bin/arch_sidecar_load` locate their migration files relative to the binary? How is the project root discovered by `arch_callgraph_ocaml` / `Arch_index.run` (`project_root`, `--build-dir`)?
6. How does `comment_db_meta` record producer facts today (`callgraph_contract`, `exn_contract`, `schema_version`, `language`): the write sites, the read sites (`Arch_db.meta`, `require_contract`), and how the tezt helpers seed or assert on it (`Fixture.main ~seed`, `Db.string_opt`)?
7. What test fixtures exist for multi-module OCaml libraries and for refusal paths in `tezt/tests/` (e.g. `exn_raise_sets.ml`'s two-unit fixture, `callgraph_soundness.ml`'s `Refuses`), and how does `tezt/lib/arch_tezt.ml` pass extra CLI flags or environment to `arch_callgraph_ocaml` / `arch_query` (`run_command ~env`, `query_raw`)?
8. [ecosystem] How do existing static analyses model error-carrying monads and result types — Rust's `Result`/`?` in clippy or rust-analyzer diagnostics, Haskell/Scala effect-tracking tools, Tezos's own `tezt`/`error_monad` conventions (`register_error_kind` categories, `TzTrace`), and academic "exception/error effect inference" for ML — and what do they report when the error value is not statically known?
9. [ecosystem] Which TOML parsing libraries exist for OCaml 5 (`toml`, `otoml`, `toml-parser`…): opam availability, TOML version supported, maintenance status, dune/opam dependency footprint, and whether any is already used by tooling in the OCaml ecosystem (dune itself, ocamlformat, odoc)?
