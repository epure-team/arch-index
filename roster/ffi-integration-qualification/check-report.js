#!/usr/bin/env node
'use strict';const cp=require('child_process'),crypto=require('crypto'),path=require('path');const root=process.argv[2]||'/home/mathias/dev/tezos/tezos';const r=cp.spawnSync(process.execPath,[path.join(__dirname,'report.js'),root],{encoding:'utf8',maxBuffer:64*1024*1024});if(r.status)process.exit(2);const x=JSON.parse(r.stdout);if(x.analysis!=='ffi_integration_qualification'||Object.values(x.components).some(c=>!['ANALYSED','NOT_ANALYSED'].includes(c.status)))process.exit(1);if(crypto.createHash('sha256').update(JSON.stringify(x.components)).digest('hex')!==x.report_digest)process.exit(1);process.stdout.write(`CHECK5_PASS: ${x.summary.ocaml_c_strict} OCaml-C, ${x.summary.rust_archive_endpoints} Rust endpoints; graph unchanged\n`);
