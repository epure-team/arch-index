# Roster intake — FFI registry aggregate report

Add a deterministic, read-only report over the registry-gated FFI sidecar
envelope. It records the registry digest and per-connector availability without
creating a baseline, policy gate, SQLite row or call-graph claim. The next,
separate intake owns recurring comparison and compatibility refusal.
