# Root corrective verification

2026-09-13; active guard worktree; base HEAD87d030a plus the subsequently committed
R1 fixture/oracle/text-path correction. This is implementation evidence, not R2 GO.
All commands below have prefix `rtk proxy`; compiler-backed commands additionally
use `opam exec --switch=/home/mathias/dev/arch-index --`.

- `dune build --root .`: exit0 after all specialist edits.
- `dune test --root . --force`: session62905, terminal exit0,256/256Tezt and64
  Alcotest (3+8+17+10+7+5+14). Complete collected output retained in
  correction-full-suite.log, ANSI color sequences removed only.
- All six `node scripts/check-arch-guard.js MODE` calls, standalone context
  ratchet, and three `checks/origin-recurring-consumer.js` modes: exit0.
  Assertion/execution controls:1/2 respectively. Exact outputs in
  correction-checker-modes.jsonl. Modes ran independently with unchanged built
  binaries while the full suite finished; no concurrent Dune invocation.
- Fresh self database `/tmp/guard-correction-self.wDUGvK/self.db`, generated by
  `_build/default/bin/arch_callgraph_ocaml/arch_callgraph_ocaml.exe
  --build-dir=_build/default/lib/arch_index
  --db-path=/tmp/guard-correction-self.wDUGvK/self.db
  --schema-path=architecture-schema.sql`: exit0,23modules/828functions/5223calls.
  CI SQL output compared byte-for-byte to test/fixtures/self-index-stats.txt:0.
- `./arch-rules /tmp/guard-correction-self.wDUGvK/self.db arch-rules.txt
  --on-vacuous fail`:0,one proved/threeUNKNOWN/zero failures.
- `node scripts/origin-consumer.js --out /tmp/guard-correction-self.wDUGvK/origin`
  and `node scripts/origin-consumer-artifacts.js` on that same output: both0,
  held UNKNOWN, no drift. Compact provenance/digests in correction-self-summary.json.
- `git diff --check`, scope manifest gate,22-file review-bundle verification:0.
  Fetched origin/main remains77c7691436a716bfec503e22f1649a4303179fcc.

Fresh committed recalibration and committed-range impact still required after
the product checkpoint. Original pin430/headroom25/query/reference values remain
unchanged. No Tezos scan, formal proof, or production precision gain is claimed.

The root rejected an agent proposal to encode the newline-path defect as an
expected failure. The positive FR-043 test remained RED until production quoted
paths correctly. Numeric semantics did not change. Root also rejected generic
fabricated unsafe coordinates; the oracle now accepts an independently computed
exact decimal only for the explicitly constructed max-column fixture.
