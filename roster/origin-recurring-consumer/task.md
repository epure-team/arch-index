# Task — origin-recurring-consumer

## User direction (verbatim excerpts)

« ok, tu peux lancer ce plan, en utilisant $roster-run et la suite roster complete (en autonomie). Lorsqu'une étape est complete et a passé le pipeline roster, tu peux en faire une PR, attendre qu'elle soit verte en corrigeant ce qui doit l'etre si besoin et merger avant de passer a l'étape suivante. N'oubblie pas de mettre a jour la note roadmap aussi »

« attention a ne pas conserver des worktrees inutiles (et leurs artefacts de build sur le disque) »

## Approved queue slice (roadmap-derived, not a new verbatim user quote)

Real recurring consumer: execute `forbid origin` against a pinned, bounded reference corpus
with explicit coverage; exercise positive and negative cases. Prioritize owned project code
and bounded fixtures, not third-party offensive audits. Reuse shipped rules/report APIs.
Measure useful reviewer questions and effort, not only graph-edge counts. Full pipeline and
sequential green-CI merge before the separate arch-guard experiment. PR99 already merged.

No analyzer-value implementation, no third-party campaign, no vendor harness changes in this
slice. Corpus choice and concrete assertions will be frozen after neutral research/intake.

Questions generated by reused Sol subagent without tools, reviewed by root under the user's
standing autonomy; no new human question-by-question approval or fresh agent context claimed.
