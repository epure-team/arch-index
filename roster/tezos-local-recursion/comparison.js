'use strict';
// Reuse the shipped counted callback-TOP -> stored bounded-target algebra.
// Permission remains task-specific: witness.js must independently certify each
// recursive occurrence before any pair reaches this pure multiset comparator.
module.exports = require('../tezos-open-bodies/comparison.js');
