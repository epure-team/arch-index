# Plan voices — intake-only decomposition

## Voice1 Sol (substitute for unavailable Claude)

Independent intake-only analysis completed before voice2 dispatch. Reused worker
due thread quota; no code/spec mining or writes/builds requested.

Sequence proposed: baseline/guard -> native positive+refusal oracle -> private
identity resolution -> arity/residual controls -> rich/flat preservation ->
adversarial witness controls -> fixed410 comparison -> full delivery gates.
Risks: ordinary-table/MUST leakage, guessed ordinal, homonym/cross-artifact matches,
RHS scope escape, dropped storage, omitted-argument counting, aggregate-only
comparison, capacity reuse, frozen baseline edits and parallel contamination.
No contractual open question; empirical RED/preservation/witness/gain questions
must be settled before retention.

Main sequencing correction: all executable checker scaffolding and mandatory
boundary assertions must precede product edits, not be deferred until after the
happy-path change. Subsequent green runs exercise the same focused capability,
not independently merged horizontal layers. Conditional self-reference refresh
is only investigated if candidate guards require it; no speculative refresh.
Pre-existing protected dirt must match the recorded state, not be removed.

## Voice2 Terra

Independent intake-only analysis ran sequentially after voice1, without its output.
Sequence: freeze executable admissibility/refusal boundaries -> frozen source/input
baseline -> complete native identity/arity/preservation controls -> private collector
admission -> availability/residual correctness -> serialized verification/delivery.
No contractual questions or alternate direction. Same risks as voice1; emphasizes
all controls before product code and no early ordinary-table insertion.

## Consensus

| Point | Voice1 | Voice2 | Status |
|---|---|---|---|
| Baseline and full pre-product guard | Required | Required | AGREE |
| Native actual-identity oracle and real RED | Required | Required | AGREE |
| Invocation-only private path, no ordinary early seed | Required | Required | AGREE |
| Actual stored body/ordinal and no new MUST | Required | Required | AGREE |
| Complete preservation/capacity controls | Required | Required | AGREE |
| Controls before product edit | Some extensions listed later | All controls first | AGREE after applying explicit spec/intake sequencing constraint; not alternative product designs |
| Conditional self-reference calibration | Only if needed | Only if needed | AGREE |
| Positive fixed410 proof then full roster/CI/merge | Required | Required | AGREE |

No DISAGREE on reachable design choices and no USER-CHALLENGE recommendation.
Internal test/product/verification steps are one end-to-end iteration and one PR,
not separately merged horizontal layers or additional loop iterations.
