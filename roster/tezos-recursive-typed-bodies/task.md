# Task — tezos-recursive-typed-bodies

## User authorization (verbatim excerpts)

“ok et continue en autonomie, teste parfois sur tezos protocole pour voir si ça améliorer les données ou sur irmin dans tezos (beaucoup de foncteurs)”

“ok continue en /loop, l'objectif est une $improvement-loop qui a pour objectif d'émaliorer la resolution dans les cas tezos (irmin et protocole) en passant par roster bien sur”

“non je propose 5 itérations”

“ok ok continue”

## Selected investigation within that authorization

Fifth and final product attempt, not five additional attempts. Four retained deliveries are PR105–108. Current retained counts: Irmin 4849, protocol 12157, canonical rows 45052. Current canonical digest: 082bdce92c6cab7b654f87a90db59ca9979d7004f45a42997a33718f6cd7c67a.

Investigate exact local singleton recursive calls whose compiler representation includes locally abstract type syntax and whose immediate RHS is not a direct Texp_function. No product change is authorized by this investigation artifact alone: research, explicit contract, plan, frozen baseline and guards precede implementation.

Retained residual evidence: 14 native occurrences across seven singleton bindings remain callback_param MAY_TOP after PR108. Five make_proof_argument heads and seven aux heads in script_ir_translator; two build_handler heads in storage_description. These are candidate occurrences, not promised gains or distinct relation counts. Mutual groups (35 other occurrences), arbitrary alias patterns and arbitrary computed RHS remain outside this selection. Initial researcher reporting said eight bindings; direct enumeration corrected that arithmetic error to seven.

Actual compiler-only observation at 02:00:38 UTC confirms that all seven RHSs are exactly open(ident)->function, with newtype/constraint extras. Locally abstract type annotations are not the blocking expression descriptor. The focused candidate is therefore a single named-path local open around a direct function RHS, with the same singleton/exact-identity restrictions; not general wrapper peeling.

Use full roster. Preserve exact compiler identity, scope, physical allocation identity, arity and conservative residuals; no new MUST, schema or public API changes. Preserve all prior relations and non-resolution metadata. Keep only a mechanically verified positive distinct-relation gain on the same pinned 410-CMT corpus, with full guards passing. PR requires exact-head green required CI and guarded rebase merge. Update external roadmap/results. Preserve unrelated user files and foreign worktrees; no disposable worktree currently needed.

Routine question-review approval is covered by the user's standing explicit autonomy; no new approval or quiz response is represented as having occurred. Existing negative contracts must be explicitly considered in the new specification, never silently weakened.
