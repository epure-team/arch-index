# Adversarial resolutions — all8 resolved from scope/source

Fresh Sol raised C1–8 after stories; no user-choice escalation or direction
change is required. Eight clarification items, zero questions asked of user.

| ID/story | Challenge | Resolution |
|---|---|---|
| C-1 US-1 | Prior art exposes UID/shapes; why binder identity? | Product resolution is per-CMT only and consumes the compiler's resolved Pident binding identity, never cross-file persistence. UID/shapes are independent endpoint evidence, not a replacement owner proof. The native witness must bind call occurrence to alias declaration, its bare RHS Ident to an actual function-body binding, and positions/arity to DB endpoint. Any competing UID/body evidence refuses comparison. No fallback from disagreement to names. |
| C-2 US-1 | Eligibility form/path/type? | All three: plain Tpat_var export, RHS exp_desc=Texp_ident(Path.Pident id,...), syntactic arrow RHS type, and id in unchanged local function-body table. exp_extra constraints leave the descriptor eligible; an actual wrapper/let/open/computation descriptor refuses. Opens already compiled into a qualified Pdot refuse. Primitives/external/absent-body IDs refuse. |
| C-3 US-1 | Shadowing at which position? | RHS resolved Ident fixes target at alias declaration; call path fixes owner/export. Every member name remains source-order masked. Later shadowing of target name cannot retarget alias. Later masking of alias export removes eligibility. The actual target's pre-existing ordinal-qualified stored name is used. |
| C-4 US-1 | Exact partial/return observations? | Each supported applied occurrence emits exactly one head to actual body, MAY_ENUMERATED, edge_form NULL and null TOP fields. Pending partial is true when result syntactically arrow or count(Some arguments)<body syntactic arity, otherwise follows existing collector rule. Required/optional label holes do not count; compiler Some defaults do. Overapplication count(Some)>arity adds exactly one existing-form callback_param *TOP* residual with NULL target, edge_form NULL and anchor=site. Partial controls add none. Verify pending metadata with native API probe: schema has no stored arity/partial column to invent. |
| C-5 US-1 | Consumers and config semantics? | Applied/callback/letop alias body edges are MAY_ENUMERATED, ordinary form NULL. Callback still conditionally denotes a possible invocation; letop retains operand ordering and conditional continuation, with existing add_path_call partial default (no new letop arity algorithm). Dead/conditional flags and scope/channel links stay as before. Configuration-head classification unchanged. Every point-free row/output unchanged, whether currently resolved, unresolved or immediate predecessor. Flat output keeps body display but callee_file only if same-file symbol exists; no invented kind/TOP columns. |
| C-6 US-2 | Which baseline is authoritative? | Attempt2 predecessor is fresh DB produced from merged PR105 product, main ace388f7a598dc762a6df6a3bac55de8d162361c (later8f309bc docs only), producer SHA c953970a15b64d026f7b502ba5d61be537c3ab5b51e219435b4a3483d57764b8, fixed410 manifest9e38880a52f855c58081b1a171af5879058cbaa2697aaa5694827cbd16f8ecd1, canonical45052 rows SHA90e76d6b40b2d7f108fcc25523f85de1cb533a98565a8a7d6d284c1654939d4b, relation4772/11615. Bind new exact DB path plus schema/producer/input/source provenance; reproduce neutrally before product edits. Original baseline retains45018/4ce3270... and is historical, never attempt2 authority. DB surrogate IDs/bytes may vary; canonical facts/provenance cannot. Any mismatch stops setup. |
| C-7 US-2 | What changed rows are permitted? | Only non-value_alias module_param MAY_TOP/no-target -> same-file actual-body MAY_ENUMERATED/null-TOP heads, with same caller/site/form and individually paired one-hop witnesses. Each witness consumes one removed and one added canonical occurrence; identical duplicates require multiplicity, no set reuse. Separately witnessed count(Some)>arity return residuals as C4 may be added once per applied head. Everything else is unchanged, including all point-free facts; any existing relation loss, new MUST or unconsumed row refuses. Gains count only new distinct relation tuples and are partitioned by slice. |
| C-8 US-2 | Exit mapping? | Well-formed comparisons with forbidden/unaccounted row movement or relation loss/new MUST/point-free change return1. Missing/malformed inputs, wrong pinned predecessor/provenance/digest, corpus/run incompleteness, malformed/position-mismatched witness and tool/setup failures return>=2 (normally2). Self-contained native assertion failures return1 only after actual setup succeeds; native compile/load failures return>=2. All nonzero outcomes deny keep. Positive controls required for each validator family. |

EC1 absent body -> old refusal; EC2 shadowing -> original target ordinal;
EC3 omitted labels -> C4; EC4 missing flat symbol -> display retained/fileNULL;
EC5 applied/point-free separation -> C5; EC6 different DB bytes -> C6;
EC7 duplicate one-witness shortfall ->1; EC8 allowed return residual plus gain
may pass comparison, but only downstream guards/review/QA/CI/merge authorize keep.

Cross-spec: body-only table and point-free immediate-predecessor requirements
stay canonical. This iteration explicitly extends only iteration1's value-member
refusal boundary (FR003/EC2) for supported invocation sites, without modifying
its historical spec/witness. LocalStructureTarget, ResolutionRelation and
ResolutionRowMultiset definitions retain exactly their previous meanings.
