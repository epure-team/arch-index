#!/usr/bin/env node
'use strict';const assert=require('assert');const {digest,report}=require('./ffi-registry-report.js');
const input={schema_version:1,analysis:'ffi_connector_sidecars',registry_digest:'b'.repeat(64),connectors:[{id:'z',mechanism:'callback_registration',version:1,status:'FRONTIER',reason:'callback_receiver_set_not_proved'},{id:'a',mechanism:'ocaml_c_primitive',version:1,status:'COMPUTED',summary:{resolved_strict:1,frontiers:2}},{id:'off',mechanism:'rust_c_archive_endpoint',version:1,status:'NOT_ANALYSED',reason:'connector_disabled'}]};
const x=report(input);assert.deepStrictEqual(x.connectors.map(r=>r.id),['a','off','z']);assert.deepStrictEqual(x.summary,{connectors:3,computed:1,not_analysed:1,refused:0,frontiers:1});const y={...x};delete y.report_digest;assert.equal(x.report_digest,digest(y));assert.throws(()=>report({...input,registry_digest:null}),/invalid/);process.stdout.write('FFI_REGISTRY_REPORT_CHECK_PASS: registry identity and availability states remain explicit\n');
